[project]
name = "smartmeter-utility"
version = "0.1.0"
description = "Smartmeter Utility package"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["uv_build>=0.9.8,<0.10.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "smartmeter.utility"
